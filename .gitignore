.venv/
__pycache__/
*.pyc
dist/
*.egg-info/

# Test cache
.pytest_cache/

# Coverage artifacts
.coverage
.coverage.*
htmlcov/
coverage.xml

# Local uv source overrides (e.g. pointing pd-book-tools at a local path)
uv.toml
Makefile.local

# Claude Code session state — agent memory canonical home is
# /workspaces/ocr-container/.claude/agent-memory/, not per-repo. If you
# see a .claude/ tree here, it is leakage; clean it up.
.claude/

# Local git worktrees created for repo-scoped agent work.
.worktree/
.worktrees/


# AI=1 make output capture log
.ci-ai.log

# local-dev mode marker (written by scripts/local-dev.sh)
.venv/.pd-local-mode
